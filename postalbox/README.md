Это обучающий проект, реализующий **MVP (Minimum Viable Product)**
автоматического постамата для получения заказов.

## 🎯 Назначение

- Пользователь делает заказ в "маркете".
- Курьер привозит заказ в постамат.
- Пользователь получает **СМС с кодом**.
- Пользователь вводит код — ячейка открывается, заказ выдан.

## 🏗 Архитектура

### Модели (`models`)

- `Order` — заказ с уникальным номером и статусом.
- `Client` — клиент, у которого есть заказ.
- `Status` — перечисление статусов заказа (например, `CREATED`, `IN_DELIVERY`, `DELIVERED`).

### Репозитории (`repositories`)

- `OrderDB` — хранит список заказов в памяти.
- `ClientDB` — хранит список клиентов в памяти.

### Сервисы (`services`)

- `OrderService` — позволяет сохранять, отменять, удалять, искать заказы (скорее менять статусы).
- `MarketService` — управляет созданием и отменой заказов.
- `ClientService` — позволяет сохранять, искать и удалять клиентов.
- `PostalBoxService` — управляет ячейками постамата: кладёт заказ в ячейку и выдаёт его по СМС-коду.

### Вспомогательные классы

- `UserNotificationApi` — "имитирует" отправку СМС пользователю.
- `TestPostalBox` — пример использования `PostalBoxService`.

##  Как запустить

Запусти класс `TestPostalBox`. Сейчас он:
- Создаёт постамат на 3 ячейки.
- Добавляет 3 заказа.
- Выводит СМС-коды.
- Забирает 2 заказа по коду.

##  Заметки

- В текущей версии данные хранятся **в памяти** (не в настоящей базе данных)!
- Ячейки не имеют физического размера!!! — один заказ = одна ячейка.
- Заказ может ждать в ячейке "вечно"!!!

##  Что можно сделать ещё?

- Добавить тесты (например, с помощью JUnit).
- Сделать настоящую базу данных.
- Реализовать логику отмены заказа в постамате.
- Добавить ограничение по времени хранения.